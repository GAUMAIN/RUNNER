# Tests

- **Unit tests** for pure logic (XP curves, economy math, rarity rolls).
- **Integration tests** for networking flows (run via S&box test harness).

Test projects to be added once a `.csproj` is generated by S&box.
