# tools

Local dev tools and scripts. Anything that helps you work on the project but isn't shipped with the game.

Examples (TBD):
- `economy_simulator.py` — Monte-Carlo balance check for currency curves
- `save_inspector.cs` — pretty-print a player save file
- `level_validator.cs` — verify checkpoints in a race scene
